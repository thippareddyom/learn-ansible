- name: sample
  hosts: localhost
  connection: local
  vars:
    URL: play.google.com
  tasks:
    - name: print url
      vars:
        URL: tasks.google.com
      ansible.builtin.debug:
        msg: "{{ URL  }}"
  